apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-claim              # PVC name (used by Pods)
  namespace: devops            # Namespace

spec:
  accessModes:
    - ReadWriteOnce            # Must match PV access mode

  resources:
    requests:
      storage: 500Mi           # Storage requested from PV
